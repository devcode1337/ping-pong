# ❓ Часто задавані питання (FAQ)

## 🚀 Встановлення та запуск

### П: Як встановити всі залежності?

**А:** Запустіть одну команду:
```bash
pip install pygame numpy Pillow
```

Або використайте скрипт:
```bash
python setup.py
```

### П: Де скачати текстури та звуки?

**А:** Вони генеруються автоматично! Просто запустіть:
```bash
python assets_generator.py
```

### П: Сервер не запускається. Що робити?

**А:** 
1. Переконайтесь, що порт 8080 вільний
2. Запустіть сервер ДО запуску клієнтів
3. Перевірте, чи немає помилок у консолі

### П: Клієнт не підключується до сервера

**А:**
1. Переконайтесь, що сервер запущено (`python server.py`)
2. Чекайте в консолі повідомлення про вдачу
3. Введіть ім'я та натисніть Enter

## 🎮 Гра

### П: Як змінити скін м'яча або платформи?

**А:**
1. У головному меню натисніть "Магазин" або "Налаштування"
2. Оберіть потрібний скін
3. Натисніть кнопку вибору
4. Скін буде застосований при наступній грі

### П: Як ввести своє ім'я?

**А:**
1. На екрані "Налаштування" введіть ім'я з клавіатури
2. Натисніть Enter для запуску гри

### П: Як перезапустити гру?

**А:**
1. Завершіть поточну партію
2. На екрані перемоги натисніть "K"
3. Повернетесь в головне меню

## 🔊 Звуки

### П: Звуки не грають

**А:**
1. Перевірте наявність numpy: `pip install numpy`
2. Перевірте гучність в операційній системі
3. Перевірте, чи включена аудіо у коді

### П: Звуки дуже гучні/тихі

**А:**
Змініть значення `volume` у файлі `client.py`:
```python
audio_manager.set_volume(0.5)  # 0.0 - 1.0
```

## 🎨 Персоналізація

### П: Як додати новий скін?

**А:**
1. Відредагуйте `assets/config/skins.json`
2. Додайте новий запис:
```json
{
  "id": "ball_purple",
  "name": "Фіолетовий м'яч",
  "color": [200, 0, 200],
  "price": 150,
  "unlocked": false
}
```
3. Запустіть `assets_generator.py`

### П: Як змінити кольори кнопок?

**А:**
Відредагуйте `ui_manager.py` і знайдіть `Button(..., color=(R, G, B))`

Приклад:
```python
Button(..., color=(255, 100, 100))  # Червоні кнопки
```

## 📊 Продуктивність

### П: Гра гальмує

**А:**
1. Закрийте інші програми
2. Перевірте системні ресурси (диск, RAM)
3. Зменшіть FPS у `client.py`:
```python
clock.tick(30)  # Замість 60
```

### П: Велика кількість текстур сповільнює запуск

**А:**
Це нормально при першому запуску. Текстури кешуються в пам'яті.

## 🔧 Налаштування

### П: Як змінити розмір вікна?

**А:**
Змініть у `client.py`:
```python
WIDTH, HEIGHT = 1024, 768  # Замість 800, 600
```

### П: Як змінити FPS?

**А:**
Змініть у `client.py`:
```python
clock.tick(120)  # Замість 60
```

## 🌐 Мережеві питання

### П: Як запустити на різних комп'ютерах?

**А:**
1. Запустіть сервер на одному комп'ютері
2. Отримайте IP адресу сервера: `ipconfig` (Windows)
3. У файлі `client.py` змініть:
```python
client.connect(('IP_АДРЕСА', 8080))
```
4. Запустіть клієнтів на обох комп'ютерах

### П: Сервер потребує пароль?

**А:**
Ні, це локальна мережа без автентифікації. Переконайтесь, що обидва комп'ютери в одній мережі.

## 📝 Редагування коду

### П: Як додати новий звук?

**А:**
В `audio_manager.py`:
```python
# Додайте у _generate_sounds()
self.sounds['new_sound'] = SoundGenerator.generate_beep(
    frequency=1000,  # Герци
    duration=100     # Мілісекунди
)
```

Потім використайте:
```python
audio_manager.play_sound('new_sound')
```

### П: Як змінити швидкість м'яча?

**А:**
Це контролює сервер у `server.py`. Зверніться до розробника сервера.

## 🐛 Поширені помилки

### "ModuleNotFoundError: No module named 'pygame'"

**Рішення:**
```bash
pip install pygame
```

### "FileNotFoundError: assets/images/..."

**Рішення:**
```bash
python assets_generator.py
```

### "Connection refused" 

**Рішення:**
1. Запустіть `python server.py`
2. Чекайте 2-3 секунди перед запуском клієнтів

## 💡 Поради

1. **Бекап конфіга**: Скопіюйте `skins.json` перед змінами
2. **Тестування**: Запускайте на одному комп'ютері спочатку
3. **Логування**: Перевіряйте консоль для помилок
4. **Документація**: Прочитайте `DOCUMENTATION.md`

## 🎯 Контакт та допомога

Якщо у вас виникла проблема:

1. Перевірте цей FAQ
2. Прочитайте `README.md` та `DOCUMENTATION.md`
3. Перевірте консоль для повідомлень про помилки
4. Спробуйте перезапустити програму

---

**Останнє оновлення**: 28 грудня 2025  
**Версія документації**: 1.0
